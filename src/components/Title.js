const Title = () => {
  return (
    <div>
      <h1>Covid Tracker</h1>
    </div>
  );
};

export default Title;

/* <Route path="/" element={ < TopPage countriesJson={countriesJson} /> } />
newConfirmed: data[ data.length -1].Confirmed - data[ data.length -2], */
